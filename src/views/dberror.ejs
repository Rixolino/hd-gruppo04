<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Errore Database - HelpDigit</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>
    <div class="container py-5 text-center">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card border-0 shadow-lg">
                    <div class="card-body p-5">
                        <div class="mb-4">
                            <i class="fas fa-database text-danger fa-4x mb-3"></i>
                            <h1 class="display-5 fw-bold text-danger">Errore Database</h1>
                        </div>
                        
                        <p class="lead mb-4">Il nostro sistema è temporaneamente sovraccarico.</p>
                        <p>Stiamo lavorando per risolvere il problema il più rapidamente possibile.</p>
                        
                        <div class="d-grid gap-2 d-sm-flex justify-content-center mt-4">
                            <a href="/" class="btn btn-primary px-4 mr-sm-3">
                                <i class="fas fa-home mr-2"></i>Torna alla home
                            </a>
                            <button onclick="window.location.reload()" class="btn btn-outline-secondary px-4">
                                <i class="fas fa-sync-alt mr-2"></i>Riprova
                            </button>
                        </div>
                        
                        <div class="mt-5 pt-3 border-top">
                            <p class="text-muted mb-2">Hai bisogno di aiuto?</p>
                            <a href="/contact" class="btn btn-link">Contatta il supporto</a>
                        </div>
                        
                        <% if (typeof errorDetails !== 'undefined') { %>
                        <div class="mt-4 pt-3 border-top">
                            <button class="btn btn-sm btn-outline-secondary" type="button" data-toggle="collapse" data-target="#errorDetails">
                                Dettagli tecnici
                            </button>
                            <div class="collapse mt-3" id="errorDetails">
                                <div class="card card-body bg-light text-left">
                                    <code><%= errorDetails %></code>
                                </div>
                            </div>
                        </div>
                        <% } %>
                    </div>
                </div>
                
                <div class="mt-4 text-muted">
                    <small>Codice errore: <%= typeof errorCode !== 'undefined' ? errorCode : 'DB001' %></small>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.min.js"></script>
    <script src="/js/build.js"></script>
</body>
</html>
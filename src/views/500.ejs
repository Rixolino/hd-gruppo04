<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Errore del Server - HelpDigit</title>
    <!-- Bootstrap e CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-lg border-0">
                    <div class="card-body p-5">
                        <div class="text-center mb-4">
                            <i class="fas fa-server text-danger" style="font-size: 4rem;"></i>
                        </div>
                        <h1 class="text-center mb-3">Errore del Server</h1>
                        <p class="text-center text-muted mb-4">
                            Si è verificato un errore interno del server. Ci scusiamo per l'inconveniente.
                        </p>
                        <div class="alert alert-danger">
                            <%= typeof message !== 'undefined' ? message : 'Si è verificato un errore interno. Il team tecnico è stato avvisato.' %>
                        </div>

                        <!-- Dettagli errore in modalità sviluppo -->
                        <% if (process.env.NODE_ENV === 'development' && typeof error !== 'undefined' && error) { %>
                            <div class="alert alert-secondary mt-4">
                                <h5 class="mb-3"><i class="fas fa-bug me-2"></i>Dettagli tecnici:</h5>
                                <pre class="mb-0 p-3 bg-light"><%= error.stack || JSON.stringify(error, null, 2) %></pre>
                            </div>
                        <% } %>

                        <div class="text-center mt-5 d-flex flex-wrap justify-content-center gap-3">
                            <a href="/" class="btn btn-primary">
                                <i class="fas fa-home me-2"></i> Torna alla Home
                            </a>
                            
                            <a href="javascript:window.location.reload()" class="btn btn-outline-secondary">
                                <i class="fas fa-sync-alt me-2"></i> Riprova
                            </a>
                            
                            <!-- Pulsante logout visibile solo per utenti autenticati o quando richiesto -->
                            <% if (locals.user || locals.showLogout === true) { %>
                                <a href="/auth/logout" class="btn btn-danger">
                                    <i class="fas fa-sign-out-alt me-2"></i> Logout
                                </a>
                            <% } %>
                        </div>
                    </div>
                </div>

                <!-- Suggerimenti utili -->
                <div class="card mt-4 border-0 bg-light">
                    <div class="card-body">
                        <h5><i class="fas fa-info-circle me-2"></i>Suggerimenti:</h5>
                        <ul class="mb-0">
                            <li>Prova ad aggiornare la pagina</li>
                            <li>Verifica la tua connessione internet</li>
                            <li>Torna più tardi se il problema persiste</li>
                            <li>Se hai bisogno di assistenza immediata, contatta il supporto</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <br>
    <br>
    <br>
    <!-- JavaScript -->
    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.bundle.min.js"></script>
    <!-- Include build.js per garantire funzionalità dell'interfaccia -->
    <script src="/js/build.js"></script>
</body>
</html>
error.ejs -->
<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Errore' %> - HelpDigit</title>
    <!-- Inclusione CSS -->
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <!-- La navbar verrà inserita qui da build.js -->
    
    <main class="container py-5">
        <div class="row">
            <div class="col-md-8 mx-auto text-center">
                <div class="card shadow-sm border-0">
                    <div class="card-body p-5">
                        <div class="mb-4">
                            <i class="fas fa-exclamation-circle text-danger" style="font-size: 4rem;"></i>
                        </div>
                        <h1 class="display-4 mb-4"><%= title || 'Errore' %></h1>
                        <p class="lead mb-4"><%= errorMessage || 'Si è verificato un errore imprevisto.' %></p>
                        
                        <% if (typeof error !== 'undefined' && error) { %>
                            <div class="alert alert-danger text-left">
                                <pre class="mb-0"><%= error %></pre>
                            </div>
                        <% } %>
                        
                        <div class="mt-5">
                            <a href="/dashboard" class="btn btn-primary">
                                <i class="fas fa-home mr-2"></i>Torna alla dashboard
                            </a>
                            
                            <% if (showLogout) { %>
                            <a href="/logout" class="btn btn-outline-secondary ml-3">
                                <i class="fas fa-sign-out-alt mr-2"></i>Logout
                            </a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <!-- Il footer verrà inserito qui da build.js -->
    
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/js/build.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Errore - HelpDigit</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body>
    <!-- La navbar verrà inserita qui da build.js -->
    
    <main class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card border-danger shadow-sm">
                    <div class="card-body text-center p-5">
                        <div class="mb-4">
                            <i class="fas fa-exclamation-circle text-danger" style="font-size: 4rem;"></i>
                        </div>
                        <h1 class="h3 mb-3">Si è verificato un errore</h1>
                        <p class="lead mb-4"><%= message %></p>
                        
                        <div class="d-flex justify-content-center">
                            <a href="javascript:history.back()" class="btn btn-outline-secondary mr-3">
                                <i class="fas fa-arrow-left mr-2"></i>Torna indietro
                            </a>
                            <a href="/" class="btn btn-primary">
                                <i class="fas fa-home mr-2"></i>Vai alla home
                            </a>
                        </div>
                        
                        <% if (process.env.NODE_ENV !== 'production' && error) { %>
                            <div class="mt-5">
                                <hr>
                                <details class="text-left">
                                    <summary class="text-muted">Dettagli tecnici</summary>
                                    <div class="mt-3">
                                        <pre class="bg-light p-3 rounded"><code><%= error instanceof Error ? error.stack : JSON.stringify(error, null, 2) %></code></pre>
                                    </div>
                                </details>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <!-- Il footer verrà inserito qui da build.js -->
    
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/js/build.js"></script>
</body>
</html>
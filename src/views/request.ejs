<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Richiedi Servizio - HelpDigit</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <!-- La navbar verrà inserita qui da build.js -->
    
    <main class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <h2><%= service.name %></h2>
                <p class="lead"><%= service.description %></p>
                <h4 class="mb-3">€ <%= service.price.toFixed(2) %></h4>
                <p><strong>Categoria:</strong> <%= service.category %></p>
                
                <div class="mt-4">
                    <h5>Perché scegliere questo servizio?</h5>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-check-circle text-success mr-2"></i> Professionalità garantita</li>
                        <li><i class="fas fa-check-circle text-success mr-2"></i> Consegna puntuale</li>
                        <li><i class="fas fa-check-circle text-success mr-2"></i> Assistenza post-servizio</li>
                        <li><i class="fas fa-check-circle text-success mr-2"></i> Soddisfatti o rimborsati</li>
                    </ul>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">Richiedi questo servizio</h4>
                    </div>
                    <div class="card-body">
                        <form action="/services/request" method="POST">
                            <input type="hidden" name="serviceId" value="<%= service.id %>">
                            
                            <div class="form-group">
                                <label for="details">Dettagli della richiesta:</label>
                                <textarea class="form-control" id="details" name="details" rows="5" required placeholder="Descrivi nel dettaglio le tue esigenze..."></textarea>
                            </div>
                            
                            <div class="form-group">
                                <label>Seleziona la priorità:</label>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="priority" id="standard" value="standard" checked>
                                    <label class="form-check-label" for="standard">Standard (5-7 giorni lavorativi)</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="priority" id="express" value="express">
                                    <label class="form-check-label" for="express">Express (2-3 giorni lavorativi, +30%)</label>
                                </div>
                            </div>
                            
                            <button type="submit" class="btn btn-primary btn-lg btn-block">
                                <i class="fas fa-paper-plane"></i> Invia richiesta
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <!-- Il footer verrà inserito qui da build.js -->
    
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/js/build.js"></script>
</body>
</html>